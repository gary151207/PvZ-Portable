# 斗蛐蛐 (Cricket Fight) 新小游戏设计

**日期:** 2026-08-17
**状态:** 待实现
**类型:** 新 GameMode（小游戏挑战）

## 目标

新增一个小游戏挑战「斗蛐蛐」：5 个随机植物 vs 6 个随机僵尸，纯自动战斗，玩家全程观战。每次进入完全随机重掷，胜负难料 —— 呼应斗蛐蛐的观赏性与不确定性。

## 核心玩法

- **地图**：黑夜（`BACKGROUND_2_NIGHT`）
- **单线对决**：双方全部位于第 3 行（网格行索引 2，中间格线）
  - 我方：5 个随机植物，固定摆在第 1-5 列（网格列索引 0-4），每列 1 个
  - 敌方：6 个随机僵尸，从右侧进入，沿第 3 行走来
- **纯自动战斗**：
  - 植物自动攻击，不依赖阳光经济
  - 无种子栏、无铲子、无手动操作
  - 跳过选卡界面，进关自动开战
- **完全随机**：每次进入关卡重新掷全部随机量
  - 植物种类：随机池 = 技术安全的全量池（见下方排除清单）
  - 植物顺序：5 个植物在 5 个格位间随机排列
  - 僵尸种类：随机池 = 技术安全的全量池（见下方排除清单）

## 胜负规则

- **胜利**：6 只僵尸全部死亡
- **失败**：任意僵尸碰到小推车机（第 3 行左侧的边界线，不触发刈草，碰到即立即判负）
- **兜底**：气球/矿工等飞越或钻过小推车的僵尸，仍按原版「僵尸进家」判定判负
- **无尽制**：胜利（约 2 秒停留）或失败（约 2.5 秒停留）后，**直接就地重开下一场** — 清场 → 重新掷全部随机量（新 5 植物 + 新 6 僵尸），不返回菜单；首次胜利仍计入奖杯进度；玩家随时可通过菜单按钮退出

## 随机池排除清单

排除项为**技术上无法独立运作 / 会导致软锁或异常表现**的种类（非平衡保底）。平衡风险（抽到向日葵白给、抽到伽刚特尔打不过）全部保留，这是用户选择的「全量随机」设计意图。

### 植物池（SEED_PEASHOOTER(0) 至 SEED_IMITATER(48)，排除以下 7 种）

| 排除 | 原因 |
|------|------|
| `SEED_LILYPAD` | 池专属，陆地上表现异常 |
| `SEED_TANGLEKELP` | 池专属，陆地上无效果 |
| `SEED_CATTAIL` | 需要睡莲支撑 |
| `SEED_PUMPKINSHELL` | 包裹对象为空，无意义 |
| `SEED_FLOWERPOT` | 无意义 |
| `SEED_COBCANNON` | 需要双格玉米加农炮配对，单独放置可能异常 |
| `SEED_IMITATER` | 占位符，不是真实植物 |

水生植物已加入（不排除）：`SEED_LILYPAD`（陆地废卡风险）、`SEED_TANGLEKELP`（斗蛐蛐下可抓陆地僵尸，一击必杀）、`SEED_CATTAIL`（全图追踪导弹，陆地可用）。

排除后池内 45 种（含水生植物：睡莲为废卡风险；香蒲全图追踪导弹强卡；缠绕海草一击必杀——斗蛐蛐下已解除水池限制，可抓陆地僵尸）。

### 僵尸池（ZOMBIE_NORMAL(0) 至 ZOMBIE_REDEYE_GARGANTUAR(32)，排除以下 13 种）

| 排除 | 原因 |
|------|------|
| `ZOMBIE_BACKUP_DANCER` | 仅能由舞者召唤，独立生成可能异常 |
| `ZOMBIE_DUCKY_TUBE` / `ZOMBIE_SNORKEL` / `ZOMBIE_DOLPHIN_RIDER` | 池专属僵尸 |
| `ZOMBIE_BOBSLED` | 仅出现在雪橇关卡编组 |
| `ZOMBIE_YETI` | 受击后逃出关卡，导致无法判定全灭 |
| `ZOMBIE_BOSS` | 最终 BOSS 特殊行为 |
| `ZOMBIE_PEA_HEAD` ~ `ZOMBIE_TALLNUT_HEAD` | 益智模式（I, Zombie）头部变体 |
| `ZOMBIE_BALLOON` | 玩家要求移除（陆地无仙人掌+三叶草时无敌必输） |

排除后池内 19 种，含伽刚特尔/白眼伽刚特尔/玩偶匣/矿工等。

## 技术结构

| 文件 | 改动 |
|------|------|
| `src/ConstEnums.h` | 新增 `GAMEMODE_CHALLENGE_CRICKET`，放在 `NUM_GAME_MODES` 哨兵之前（遵守序列化稳定规则，不重排现有值） |
| `src/Lawn/Widget/ChallengeScreen.cpp` | `gChallengeDefs` 新增一项：页 = `CHALLENGE_PAGE_CHALLENGE`，按钮名使用字面量 `"斗蛐蛐"`（不走 `[XXX]` 字符串键，避免用户 properties 缺失时显示 `<Missing>`） |
| `src/LawnApp.h` / `src/LawnApp.cpp` | 新增 `IsCricketFightLevel()` 帮助函数；进入该模式时跳过选卡界面（`ShowSeedChooserScreen`），进关后自动开始战斗 |
| `src/Lawn/Board.cpp` | 见下方细节 |

### Board 改动细节

1. **背景**：`PickBackground()` 中为该模式指定 `BACKGROUND_2_NIGHT`
2. **初始阳光**：无阳光经济，`mSunMoney = 0`（与 Beghouled 等模式一致的处理方式）
3. **初始布阵**：`InitLevel()` 或 `StartLevel()` 时，在 (col 0-4, row 2) 随机摆放 5 个随机植物（通过 `AddPlant`，按随机顺序排列 5 个随机 SeedType）
4. **出怪**：定制 `InitZombieWaves()` —— 一波直接出 6 只随机僵尸，全部走第 3 行（固定出怪路径，绕过点数预算系统）；开局即刷出（`mZombieCountDown` 极短或直接生成）
5. **UI 隐藏**：隐藏种子栏（SeedBank）、铲子按钮、阳光计数等玩家交互元素；纯观战
6. **小推车边界**：`SetupLawnMowers()` 只为第 3 行创建 1 辆小推车（其余行不创建），作为判负边界线；过场中正常滑入显示
7. **胜负判定**：
   - 胜利：复用现有波次结算（全部波刷完且全灭 → 关卡完成）
   - 失败：`LawnMower::Update()` 的矩形重叠检测中，斗蛐蛐分支改为直接 `ZombiesWon()`（不调用 `MowZombie` 刈草）；气球/矿工等越过小推车的僵尸由原版「进家」判定兜底
8. **无尽对战**：`Board::RestartCricketMatch()` 清场并重新掷随机量；胜利路径 = `TrySpawnLevelAward` 不落奖杯币直接 `FadeOutLevel`（120 帧）→ `CheckForGameEnd` 就地重开；失败路径 = `ZombiesWon` 设 150 帧停留 → `UpdateLevelEndSequence` 就地重开

## 边界情况

- 抽到纯辅助植物（5 个向日葵）→ 必然失败，这是"完全随机"的设计意图，不做保底
- 抽到伽刚特尔等强僵尸 → 可能无法战胜，同上
- 夜间无睡眠机制（PvZ1 原版行为），日间植物在黑夜正常工作，无需额外处理
- 大蒜将僵尸引向相邻行（1/3 行，无植物驻守）→ 该僵尸直冲小推车判负，保留此风险
- 气球僵尸在无仙人掌/三叶草时无敌 → 最终碰到小推车判负，保留此风险

## 不做的事（YAGNI）

- 不做锦标赛/连胜排行榜（无尽对战，只记总胜场；玩家随时可退出）
- 不做手动干预（铲子、重新摆放、暂停操作）
- 不做难度分级或平衡保底
- 不新增美术资源（复用现有植物/僵尸精灵与黑夜背景）

## 验证

无自动化测试（项目无测试套件）。验证方式：
- 构建通过（cmake + Ninja Release）
- 手动游玩：从小游戏页进入「斗蛐蛐」→ 确认黑夜地图、5 植物摆在第 3 行 1-5 列、6 僵尸沿第 3 行进攻、无种子栏/铲子/阳光 UI
- 多次进入确认每次随机量不同
- 分别验证胜利（僵尸全灭）与失败（植物全灭 / 僵尸进家）两条结算路径
